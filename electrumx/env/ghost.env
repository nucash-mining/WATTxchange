# Ghost node RPC — fill in real credentials from ghost.conf.
# Node must run with: server=1, txindex=1, rpcallowip=<docker subnet or host>.
DAEMON_URL=http://ghostrpc:CHANGE_ME@host.docker.internal:51725/
