# Trollcoin node RPC — fill in the real credentials from your trollcoin.conf.
# The node must run with: server=1, txindex=1, rpcallowip=<docker subnet or this host>.
# host.docker.internal resolves to the Docker host (where the daemon runs).
DAEMON_URL=http://trollrpc:CHANGE_ME@host.docker.internal:9666/
